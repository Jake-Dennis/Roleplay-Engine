/**
 * FileTree component tests have been moved to:
 *   src/components/__tests__/file-tree.test.tsx
 *
 * This file exists for backward compatibility.
 * See the canonical test file in src/components/__tests__/ for comprehensive coverage.
 */

import { describe, it, expect } from "bun:test";

describe("file-tree component", () => {
  it("has placeholder redirecting to canonical tests", () => {
    expect(true).toBe(true);
  });
});